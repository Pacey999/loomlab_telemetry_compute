; FTCAN 2.0 gateway — generic header + raw frames (no mini_decode).
[platformio]
default_envs = esp32dev

[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 921600
build_flags =
    -DCORE_DEBUG_LEVEL=1
